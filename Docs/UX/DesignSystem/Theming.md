# Theming

> Theme tokens, dark mode support, and high contrast guidelines.

## Theme Architecture

```
┌─────────────────────────────────────────┐
│ Design Tokens (Semantic)                │
│   color-bg-primary, color-text-primary  │
├─────────────────────────────────────────┤
│ Theme Provider                          │
│   Resolves tokens per theme             │
├─────────────────────────────────────────┤
│ Themes                                  │
│   Light | Dark | High Contrast          │
└─────────────────────────────────────────┘
```

## Theme Tokens

### Background Colors

| Token | Light | Dark |
|-------|-------|------|
| `color-bg-primary` | #FFFFFF | #0D0D0D |
| `color-bg-secondary` | #FAFAFA | #1A1A1A |
| `color-bg-tertiary` | #F5F5F5 | #2C2C2E |
| `color-bg-elevated` | #FFFFFF | #1C1C1E |

### Text Colors

| Token | Light | Dark |
|-------|-------|------|
| `color-text-primary` | #1A1A1A | #FAFAFA |
| `color-text-secondary` | #666666 | #A1A1A6 |
| `color-text-tertiary` | #8E8E93 | #8E8E93 |
| `color-text-inverse` | #FFFFFF | #1A1A1A |

### Border Colors

| Token | Light | Dark |
|-------|-------|------|
| `color-border-default` | #E5E5E5 | #3A3A3C |
| `color-border-subtle` | #F0F0F0 | #2C2C2E |
| `color-border-strong` | #C7C7CC | #545458 |

## Dark Mode

### Automatic Detection

```swift
@Environment(\.colorScheme) var colorScheme

var backgroundColor: Color {
    colorScheme == .dark ? Color.bgDark : Color.bgLight
}
```

### Manual Toggle

```swift
@AppStorage("preferredColorScheme") var preferredScheme: ColorScheme?

.preferredColorScheme(preferredScheme)
```

## High Contrast Mode

### Enhanced Colors

| Token | Standard | High Contrast |
|-------|----------|---------------|
| `color-text-primary` | #1A1A1A | #000000 |
| `color-border-default` | #E5E5E5 | #000000 |
| `color-primary-500` | #D5815A | #B35C2A |

### Accessibility

- Minimum contrast ratio: 7:1 (AAA)
- Increased border widths: 2px minimum
- Enhanced focus indicators

## Theme Implementation

### SwiftUI

```swift
struct ThemeColors {
    static var primary: Color {
        Color("Primary", bundle: .main)
    }

    static var background: Color {
        Color("Background", bundle: .main)
    }
}
```

### Asset Catalog Structure

```
Assets.xcassets/
└── Colors/
    ├── Primary.colorset/
    │   └── Contents.json (Light + Dark)
    ├── Background.colorset/
    │   └── Contents.json (Light + Dark)
    └── ...
```

---
*Generated by Archon*